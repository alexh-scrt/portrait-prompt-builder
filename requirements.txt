# requirements.txt — Pinned pip dependencies for easy installation
# Install with: pip install -r requirements.txt

# Core web framework
flask>=3.0.0

# Testing dependencies
pytest>=8.0.0
pytest-flask>=1.3.0
